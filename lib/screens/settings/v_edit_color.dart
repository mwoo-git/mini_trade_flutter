import 'package:flutter/material.dart';

class EditColorView extends StatelessWidget {
  const EditColorView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('매수/매도 색상'),
      ),
    );
  }
}
